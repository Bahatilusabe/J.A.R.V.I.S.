fastapi==0.95.2
uvicorn==0.22.0
pyyaml==6.0
pytest==7.4.0
## The C-extension `pyahocorasick` is fast but can fail to build in some dev environments.
## If you have a proper build toolchain (C compiler, Python headers, Cython) you can
## enable it. Otherwise `ahocorapy` (pure-Python) is a portable fallback.
# pyahocorasick==1.4.0
ahocorapy
scapy==2.5.0
PyJWT>=2.6.0
pandas>=1.5.3
cryptography>=41.0.0
starlette==0.27.0

# Web/WebSocket support
python-socketio==5.9.0
python-multipart==0.0.6
aiofiles==23.2.1
websockets>=12.0

# Authentication & PQC
liboqs-python>=0.7.2
cryptography>=41.0.0
python-jose[cryptography]>=3.3.0

# CORS and middleware
python-multipart>=0.0.6

# Data validation and serialization
# FastAPI 0.95.2 depends on pydantic <2.0.0; pin to a 1.x release compatible with FastAPI
pydantic==1.10.12
marshmallow>=3.20.0

# Rate limiting and security
slowapi>=0.1.8

# Monitoring and logging
python-json-logger>=2.0.0
psutil>=5.9.0

# Database (optional, for persistence)
sqlalchemy>=2.0.0
aiosqlite>=0.19.0

# ML & AI Framework (optional)
# Note: MindSpore requires Linux for PyPI. On macOS, use: conda install -c conda-forge mindspore
# Or use Docker: docker pull mindspore/mindspore:latest-cpu
# For development on macOS, PyTorch is recommended: torch>=2.0.0
# mindspore>=2.0.0  # Uncomment when on Linux or using conda
