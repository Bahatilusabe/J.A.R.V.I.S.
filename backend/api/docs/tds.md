# tds

Purpose: Threat detection service (TDS) endpoints and alerting helpers.

Registered router prefix: `/api/tds`

Typical endpoints (examples)

- `GET /api/tds/alerts` — list threat alerts
- `POST /api/tds/ack` — acknowledge alerts
