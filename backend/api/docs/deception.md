# deception

Purpose: Deception services (honeypots, decoy endpoints) management API.

Registered router prefix: `/api/deception`

Typical endpoints (examples)

- `GET /api/deception/decoys` — list decoy assets
- `POST /api/deception/activate` — enable a deception scenario
