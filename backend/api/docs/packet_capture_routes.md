# packet_capture_routes

Purpose: Alias/documentation for packet capture routing (see `packet_capture` module docs).

Registered router prefix: `/api/packet_capture`

Typical endpoints: same as `packet_capture` (start/stop/download captures).
