# fl_blockchain

Purpose: Federated learning / federated blockchain integration endpoints.

Registered router prefix: (registered without explicit prefix in server wiring; tagged as `federated-learning`).

Typical endpoints (examples)

- `GET /fl/blockchain/status` — federated learning blockchain status
- `POST /fl/blockchain/submit` — submit a federated update
