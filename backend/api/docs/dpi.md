# dpi

Purpose: Deep Packet Inspection (DPI) query and signature management endpoints.

Registered router prefix: `/api/dpi`

Typical endpoints (examples)

- `GET /api/dpi/signatures` — list DPI signatures
- `POST /api/dpi/scan` — request a DPI scan for traffic sample
