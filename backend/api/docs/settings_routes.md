# settings_routes

Purpose: Configuration and settings-related endpoints.

Registered router prefix: `/api/settings`

Typical endpoints (examples)

- `GET /api/settings` — retrieve runtime settings
- `POST /api/settings` — update configuration
