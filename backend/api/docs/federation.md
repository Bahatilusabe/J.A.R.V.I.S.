# federation

Purpose: Federation APIs for node sync, membership, and federated operations.

Registered router prefix: `/api/federation`

Typical endpoints (examples)

- `GET /api/federation/nodes` — list federation nodes
- `POST /api/federation/sync` — trigger federation sync
