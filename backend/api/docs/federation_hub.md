# federation_hub

Purpose: Federation hub helpers for proxying and hub-level operations.

Registered router prefix: registered under `/api` (hub endpoints live under the API namespace).

Typical endpoints (examples)

- `POST /api/hub/announce` — announce a node to the hub
