# packet_capture

Purpose: Packet capture control and retrieval endpoints.

Registered router prefix: `/api/packet_capture`

Typical endpoints (examples)

- `POST /api/packet_capture/start` — start a capture
- `GET /api/packet_capture/download` — download capture archives
