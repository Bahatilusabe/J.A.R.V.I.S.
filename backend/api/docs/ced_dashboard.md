# ced_dashboard

Purpose: CED dashboard-specific endpoints and widgets.

Registered router prefix: `/api/ced` (dashboard-specific routes are registered alongside `ced`)

Typical endpoints (examples)

- `GET /api/ced/dashboard` — dashboard oriented CED data
