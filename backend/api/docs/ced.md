# ced

Purpose: CED (customer experience / change-event dashboard) endpoints used by the UI.

Registered router prefix: `/api/ced`

Typical endpoints (examples)

- `GET /api/ced/summary` — retrieve CED summary metrics
- `POST /api/ced/report` — submit a CED report
