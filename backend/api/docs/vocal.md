# vocal

Purpose: Voice/text interaction endpoints (if enabled) — handles vocal-related features.

Registered router prefix: `/api/vocal`

Typical endpoints (examples)

- `POST /api/vocal/command` — submit a vocal command
- `GET /api/vocal/status` — check vocal subsystem
