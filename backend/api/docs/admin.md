# admin

Purpose: Administrative endpoints (user management, system actions) registered under `/api`.

Registered router prefix: registered under `/api` (admin endpoints available under the API namespace).

Typical endpoints (examples)

- `POST /api/admin/user` — create or update a user
- `GET /api/admin/audit` — retrieve audit logs
