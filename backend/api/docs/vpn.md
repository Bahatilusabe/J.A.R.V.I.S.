# vpn

Purpose: VPN configuration and status endpoints used to manage site-to-site and client VPNs.

Registered router prefix: `/api/vpn`

Typical endpoints (examples)

- `GET /api/vpn/status` — VPN subsystem status
- `POST /api/vpn/config` — submit VPN configuration updates
