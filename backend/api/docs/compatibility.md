# compatibility

Purpose: Compatibility and integration test endpoints (used to verify interoperability with external components).

Registered router prefix: registered under `/api`.

Typical endpoints (examples)

- `GET /api/compat/check` — run compatibility checks
