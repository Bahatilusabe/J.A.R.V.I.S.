# metrics

Purpose: Metrics and telemetry query endpoints.

Registered router prefix: `/api/metrics`

Typical endpoints (examples)

- `GET /api/metrics/query` — run a metrics query
- `GET /api/metrics/health` — metrics subsystem health
