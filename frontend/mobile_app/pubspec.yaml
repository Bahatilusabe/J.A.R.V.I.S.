name: jarvis_mobile
description: J.A.R.V.I.S. SOC mobile app (minimal scaffold)
publish_to: "none"

environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^0.13.6
  web_socket_channel: ^2.3.0
  local_auth: ^2.1.3
  flutter_secure_storage: ^8.0.0
  share_plus: ^6.3.0
  flutter_sound: ^9.2.13
  path_provider: ^2.0.0
  flutter_riverpod: ^2.0.0
  fl_chart: ^0.60.0

flutter:
  uses-material-design: true

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
